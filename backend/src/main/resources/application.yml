server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/project_management?useSSL=false&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: armaan.ali.dev@gmail.com
    password: azgr qvex wusp jpmm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true