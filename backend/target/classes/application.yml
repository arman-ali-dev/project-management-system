server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/project_management?useSSL=false&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: a2gpvtltd@gmail.com
    password: cllx yzpb gyef pxhc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
